# 企业级 CRM/ERP Web 应用 - 部署指南

## 📦 项目已创建文件清单

### ✅ 配置文件 (10个)
- `package.json` - 项目依赖配置
- `vite.config.js` - Vite 构建配置
- `tailwind.config.js` - Tailwind CSS 配置
- `postcss.config.js` - PostCSS 配置
- `.eslintrc.cjs` - ESLint 代码规范配置
- `.env.example` - 环境变量示例
- `.env.development` - 开发环境配置
- `.env.production` - 生产环境配置
- `.gitignore` - Git 忽略文件配置
- `index.html` - HTML 入口文件

### ✅ 文档文件 (3个)
- `README.md` - 项目说明文档
- `ARCHITECTURE.md` - 架构设计文档
- `DEPLOYMENT.md` - 部署指南 (本文件)

### ✅ 源代码文件 (19个)

**样式文件 (1个)**
- `src/styles/index.css` - 全局样式和 Tailwind 配置

**配置文件 (2个)**
- `src/config/constants.js` - 全局常量配置
- `src/config/permissions.js` - 权限配置

**状态管理 (4个)**
- `src/stores/useAuthStore.js` - 认证状态
- `src/stores/useThemeStore.js` - 主题状态
- `src/stores/useAppStore.js` - 应用状态
- `src/stores/index.js` - 状态统一导出

**工具函数 (1个)**
- `src/utils/request.js` - HTTP 请求封装

**入口文件 (3个)**
- `src/main.jsx` - 应用入口
- `src/App.jsx` - 根组件
- `src/router.jsx` - 路由配置

**组件 (5个)**
- `src/components/common/Loading/index.jsx` - 加载组件
- `src/components/common/ErrorBoundary/index.jsx` - 错误边界
- `src/components/layout/MainLayout/index.jsx` - 主布局
- `src/components/layout/Header/index.jsx` - 顶部导航
- `src/components/layout/Sidebar/index.jsx` - 侧边栏

**页面 (3个)**
- `src/pages/auth/Login.jsx` - 登录页
- `src/pages/dashboard/index.jsx` - 仪表盘
- `src/pages/error/Unauthorized.jsx` - 403 页面
- `src/pages/error/NotFound.jsx` - 404 页面

---

## 🚀 快速开始

### 第一步:安装依赖

```bash
# 进入项目目录
cd /home/claude

# 安装依赖 (推荐使用 pnpm,速度更快)
pnpm install

# 或使用 npm
npm install
```

### 第二步:启动开发服务器

```bash
# 启动开发服务器
pnpm dev

# 或
npm run dev
```

浏览器自动打开 http://localhost:5173

### 第三步:登录测试

使用以下测试账号登录:

- **管理员**: `admin` / `admin123`
- **销售**: `sales` / `123456`

---

## 📂 项目完整目录结构

```
/home/claude/
├── public/                          # 静态资源 (需手动添加)
│   ├── favicon.ico                  # 网站图标
│   └── logo.png                     # 系统 Logo
├── src/
│   ├── api/                         # API 接口封装
│   │   └── modules/                 # 按模块拆分的接口 (待完善)
│   ├── assets/                      # 资源文件
│   │   ├── images/                  # 图片资源 (待添加)
│   │   └── icons/                   # 图标资源 (待添加)
│   ├── components/
│   │   ├── common/                  # 公共组件
│   │   │   ├── Loading/             # ✅ 已创建
│   │   │   ├── ErrorBoundary/       # ✅ 已创建
│   │   │   ├── Button/              # 待创建
│   │   │   ├── Input/               # 待创建
│   │   │   ├── Table/               # 待创建
│   │   │   └── Modal/               # 待创建
│   │   ├── business/                # 业务组件 (待创建)
│   │   └── layout/                  # 布局组件
│   │       ├── MainLayout/          # ✅ 已创建
│   │       ├── Header/              # ✅ 已创建
│   │       └── Sidebar/             # ✅ 已创建
│   ├── config/
│   │   ├── constants.js             # ✅ 全局常量
│   │   ├── permissions.js           # ✅ 权限配置
│   │   ├── routes.js                # 待创建
│   │   └── theme.js                 # 待创建
│   ├── hooks/                       # 自定义 Hooks (待创建)
│   ├── pages/
│   │   ├── auth/
│   │   │   └── Login.jsx            # ✅ 登录页
│   │   ├── dashboard/
│   │   │   └── index.jsx            # ✅ 仪表盘
│   │   ├── customer/                # 待创建
│   │   ├── sales/                   # 待创建
│   │   ├── product/                 # 待创建
│   │   ├── purchase/                # 待创建
│   │   ├── report/                  # 待创建
│   │   ├── system/                  # 待创建
│   │   ├── profile/                 # 待创建
│   │   └── error/
│   │       ├── Unauthorized.jsx     # ✅ 403 页面
│   │       └── NotFound.jsx         # ✅ 404 页面
│   ├── stores/
│   │   ├── useAuthStore.js          # ✅ 认证状态
│   │   ├── useThemeStore.js         # ✅ 主题状态
│   │   ├── useAppStore.js           # ✅ 应用状态
│   │   └── index.js                 # ✅ 统一导出
│   ├── styles/
│   │   └── index.css                # ✅ 全局样式
│   ├── utils/
│   │   ├── request.js               # ✅ HTTP 请求
│   │   ├── auth.js                  # 待创建
│   │   ├── storage.js               # 待创建
│   │   ├── date.js                  # 待创建
│   │   └── export.js                # 待创建
│   ├── App.jsx                      # ✅ 根组件
│   ├── main.jsx                     # ✅ 入口文件
│   └── router.jsx                   # ✅ 路由配置
├── .env.development                 # ✅ 开发环境变量
├── .env.production                  # ✅ 生产环境变量
├── .env.example                     # ✅ 环境变量示例
├── .eslintrc.cjs                    # ✅ ESLint 配置
├── .gitignore                       # ✅ Git 忽略
├── index.html                       # ✅ HTML 模板
├── package.json                     # ✅ 项目依赖
├── postcss.config.js                # ✅ PostCSS 配置
├── tailwind.config.js               # ✅ Tailwind 配置
├── vite.config.js                   # ✅ Vite 配置
├── ARCHITECTURE.md                  # ✅ 架构文档
├── README.md                        # ✅ 说明文档
└── DEPLOYMENT.md                    # ✅ 部署文档
```

---

## ✨ 当前项目状态

### ✅ 已完成的功能

1. **项目基础架构** (100%)
   - ✅ Vite + React 18 + Tailwind CSS v3 配置完成
   - ✅ 路由配置 (React Router v6)
   - ✅ 状态管理 (Zustand)
   - ✅ HTTP 请求封装 (Axios)
   - ✅ 全局样式和主题系统

2. **核心功能** (30%)
   - ✅ 用户登录/退出
   - ✅ 主题切换 (亮色/暗黑)
   - ✅ 侧边栏导航
   - ✅ 权限配置系统
   - ✅ 响应式布局

3. **页面** (20%)
   - ✅ 登录页
   - ✅ 仪表盘 (示例)
   - ✅ 错误页面 (403/404)
   - ⏳ 其他业务页面 (占位符,待完善)

### ⏳ 待完善的功能

根据需求文档,以下模块需要完整实现:

1. **公共组件库** (优先级:高)
   - 表格组件 (支持分页、排序、筛选)
   - 表单组件 (Input、Select、DatePicker 等)
   - 弹窗组件 (Modal、Confirm、Toast)
   - 上传组件
   - 图表组件 (基于 Recharts)

2. **业务页面** (优先级:高)
   - 客户管理:列表、详情、表单、跟进管理
   - 销售管理:报价单、订单、回款、业绩统计
   - 产品库存:产品列表、库存管理、预警、盘点
   - 采购管理:供应商、采购订单、入库、统计
   - 数据统计:仪表盘、图表、自定义报表
   - 系统配置:用户、角色、部门、日志
   - 个人中心:基本信息、修改密码、偏好设置

3. **API 接口封装** (优先级:中)
   - 各模块的 API 接口文件
   - Mock 数据支持 (开发环境)

4. **工具函数** (优先级:中)
   - 日期处理、数据导出、表单验证等

5. **高级功能** (优先级:低)
   - 数据导入/导出
   - 批量操作
   - 表单草稿缓存
   - 全局搜索

---

## 🛠️ 如何继续开发

### 方案一:手动创建缺失文件

根据 `ARCHITECTURE.md` 中的数据结构和业务流程,逐步创建各模块的页面和组件。

参考现有的 `Login.jsx` 和 `Dashboard.jsx` 的代码风格。

### 方案二:使用代码生成工具

可以使用 AI 辅助工具或代码生成器,根据数据结构快速生成 CRUD 页面。

### 方案三:复制示例代码

从开源的 React Admin 项目中参考类似组件的实现,进行修改适配。

---

## 🏗️ 生产构建

### 构建命令

```bash
# 生产环境构建
pnpm build

# 查看构建产物
ls -lh dist/
```

### 构建优化检查

构建完成后,检查以下指标:

1. **包体积** - 主包应 < 500KB (gzipped)
2. **代码分割** - 是否正确分包
3. **静态资源** - 是否正确压缩

---

## 🚀 企业内网部署

### Nginx 配置示例

创建 `/etc/nginx/conf.d/crm-erp.conf`:

```nginx
server {
    listen 80;
    server_name 192.168.1.100;
    
    root /var/www/crm-erp/dist;
    index index.html;
    
    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理 (可选)
    location /api/ {
        proxy_pass http://localhost:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 部署步骤

```bash
# 1. 构建项目
pnpm build

# 2. 上传到服务器
scp -r dist/* user@192.168.1.100:/var/www/crm-erp/

# 3. 重启 Nginx
ssh user@192.168.1.100 'sudo systemctl restart nginx'
```

---

## 📝 开发注意事项

1. **代码规范**
   - 组件使用 PascalCase
   - 函数使用 camelCase
   - 常量使用 UPPER_CASE

2. **Git 提交**
   - 使用约定式提交: `feat:` `fix:` `docs:` 等

3. **性能优化**
   - 使用 React.memo 优化组件
   - 使用 useMemo 和 useCallback 优化计算
   - 路由懒加载

4. **类型安全**
   - 建议添加 TypeScript (可选)
   - 或使用 JSDoc 注释

---

## 🆘 常见问题

### 1. 启动失败 - 端口被占用

```bash
# 修改 vite.config.js
server: {
  port: 5174,  // 改为其他端口
}
```

### 2. 样式不生效

检查 `tailwind.config.js` 中的 `content` 配置是否正确。

### 3. 路由 404

确保已在 `router.jsx` 中配置路由。

### 4. 接口请求失败

检查 `.env.development` 中的 `VITE_API_BASE_URL` 配置。

---

## 📚 相关文档

- [README.md](./README.md) - 项目说明
- [ARCHITECTURE.md](./ARCHITECTURE.md) - 架构设计
- [React 官方文档](https://react.dev/)
- [Tailwind CSS 文档](https://tailwindcss.com/)
- [Vite 文档](https://vitejs.dev/)

---

## 📧 技术支持

如有问题,请联系开发团队或查阅相关文档。

**项目当前状态**: 基础架构已完成,核心功能待完善

**下一步计划**: 完善业务页面和组件库
