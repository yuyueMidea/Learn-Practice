# 企业级 CRM/ERP Web 前端应用系统

## 项目简介

这是一款面向中大型企业的一体化办公 CRM/ERP 前端系统,实现客户管理、销售跟进、产品库存、采购管理、订单结算、数据统计的全业务闭环。

**核心特性:**
- ✨ 基于 React 18.3 + Tailwind CSS v3 (JIT 模式) 构建
- 🎯 完整的企业级业务流程:客户管理、销售管理、库存管理、采购管理
- 🔐 三级权限体系:路由级、功能级、数据级权限管控
- 📊 丰富的数据可视化:销售趋势、库存分析、业绩统计
- 🎨 专业的 UI 设计:简洁高效、支持亮色/暗黑模式
- 📱 响应式布局:桌面端优先,兼顾平板端适配
- ⚡ 高性能:路由懒加载、组件懒加载、数据缓存优化
- 🛠️ 工程化标准:ESLint 规范、模块化架构、类型安全

## 技术栈

### 核心框架
- **React 18.3** - 使用函数组件 + Hooks 开发
- **Tailwind CSS v3** - JIT 模式,自定义企业级主题
- **Vite 5.1** - 快速的开发服务器和构建工具

### 状态管理
- **Zustand** - 轻量级全局状态管理
- **React Query** - 服务端状态管理和数据缓存
- **React Hook Form + Zod** - 高性能表单处理和验证

### 路由与请求
- **React Router v6** - 客户端路由管理
- **Axios** - HTTP 请求库

### 工具库
- **date-fns** - 日期处理
- **lodash-es** - 实用工具函数
- **Recharts** - 数据可视化图表
- **file-saver** - 文件下载
- **crypto-js** - 加密工具

## 项目结构

```
crm-erp-web/
├── public/                          # 静态资源
│   ├── favicon.ico
│   └── logo.png
├── src/                             # 源代码
│   ├── api/                         # API 接口封装
│   │   ├── modules/                 # 按模块拆分的接口
│   │   │   ├── auth.js              # 认证接口
│   │   │   ├── customer.js          # 客户管理接口
│   │   │   ├── sales.js             # 销售管理接口
│   │   │   ├── product.js           # 产品库存接口
│   │   │   ├── purchase.js          # 采购管理接口
│   │   │   └── ...
│   │   └── index.js
│   ├── assets/                      # 资源文件
│   │   ├── images/                  # 图片
│   │   └── icons/                   # 图标
│   ├── components/                  # 组件
│   │   ├── common/                  # 公共组件
│   │   │   ├── Button/
│   │   │   ├── Input/
│   │   │   ├── Table/
│   │   │   ├── Modal/
│   │   │   └── ...
│   │   ├── business/                # 业务组件
│   │   │   ├── CustomerSelector/
│   │   │   ├── ProductSelector/
│   │   │   └── Charts/
│   │   └── layout/                  # 布局组件
│   │       ├── Header/
│   │       ├── Sidebar/
│   │       └── Breadcrumb/
│   ├── config/                      # 配置文件
│   │   ├── constants.js             # 常量配置
│   │   ├── permissions.js           # 权限配置
│   │   ├── routes.js                # 路由配置
│   │   └── theme.js                 # 主题配置
│   ├── hooks/                       # 自定义 Hooks
│   │   ├── useAuth.js               # 认证
│   │   ├── usePermission.js         # 权限
│   │   ├── useTable.js              # 表格
│   │   └── ...
│   ├── pages/                       # 页面组件
│   │   ├── auth/                    # 认证页面
│   │   ├── dashboard/               # 仪表盘
│   │   ├── customer/                # 客户管理
│   │   ├── sales/                   # 销售管理
│   │   ├── product/                 # 产品库存
│   │   ├── purchase/                # 采购管理
│   │   ├── report/                  # 数据统计
│   │   ├── system/                  # 系统配置
│   │   └── profile/                 # 个人中心
│   ├── stores/                      # Zustand 状态管理
│   │   ├── useAuthStore.js          # 认证状态
│   │   ├── useThemeStore.js         # 主题状态
│   │   └── useAppStore.js           # 应用状态
│   ├── styles/                      # 样式文件
│   │   └── index.css                # 全局样式
│   ├── utils/                       # 工具函数
│   │   ├── request.js               # HTTP 请求封装
│   │   ├── auth.js                  # 认证工具
│   │   ├── storage.js               # 本地存储
│   │   ├── date.js                  # 日期处理
│   │   ├── export.js                # 数据导出
│   │   └── ...
│   ├── App.jsx                      # 根组件
│   ├── main.jsx                     # 入口文件
│   └── router.jsx                   # 路由配置
├── .env.development                 # 开发环境变量
├── .env.production                  # 生产环境变量
├── .env.example                     # 环境变量示例
├── .eslintrc.cjs                    # ESLint 配置
├── .gitignore                       # Git 忽略文件
├── index.html                       # HTML 模板
├── package.json                     # 项目依赖
├── postcss.config.js                # PostCSS 配置
├── tailwind.config.js               # Tailwind CSS 配置
├── vite.config.js                   # Vite 配置
├── ARCHITECTURE.md                  # 架构设计文档
└── README.md                        # 项目说明文档
```

## 核心功能模块

### 1. 用户与权限管理
- ✅ 账号登录/退出/记住密码
- ✅ 多角色权限配置 (超级管理员、销售总监、销售员工、库管员、采购专员、财务、普通员工)
- ✅ 三级鉴权:路由级、功能级、数据级
- ✅ 用户管理:增删改查、角色分配、账号启用/禁用
- ✅ 个人中心:修改信息、修改密码、主题设置

### 2. 客户管理模块
- ✅ 客户信息全生命周期管理
- ✅ 客户跟进管理:添加跟进记录、跟进提醒
- ✅ 客户分类与标签:潜在/意向/成交/流失客户
- ✅ 客户数据管理:导入/导出、数据查重、批量操作

### 3. 销售管理模块
- ✅ 产品报价单:创建/编辑/删除/导出
- ✅ 销售订单管理:订单状态流转、审核、发货
- ✅ 回款管理:回款记录、进度跟踪、逾期提醒
- ✅ 销售业绩统计:多维度统计、可视化图表

### 4. 产品与库存管理
- ✅ 产品信息管理:增删改查、分类标签
- ✅ 库存管理:出入库记录、库存预警、库存盘点
- ✅ 库存查询:实时库存、库存台账、变动日志

### 5. 采购管理模块
- ✅ 供应商管理:供应商信息、分类等级
- ✅ 采购订单管理:订单状态流转、审核、入库
- ✅ 采购入库管理:批量入库、入库差异记录
- ✅ 采购统计:采购金额、成本分析

### 6. 数据统计与报表
- ✅ 全局数据仪表盘:核心指标、趋势图、待办提醒
- ✅ 多维度可视化图表:销售趋势、客户分布、库存周转
- ✅ 自定义报表:自定义筛选、预览导出
- ✅ 数据备份与恢复

### 7. 系统配置模块
- ✅ 全局参数配置:公司名称、系统设置
- ✅ 部门与组织架构管理
- ✅ 系统日志管理:操作日志、数据修改日志
- ✅ 模板配置:报价单/订单模板

### 8. 通用辅助功能
- ✅ 全局快速搜索:跨模块搜索
- ✅ 面包屑导航:清晰路径显示
- ✅ 批量操作:批量选择、修改、删除、导出
- ✅ 表单草稿缓存:自动保存草稿

## 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0 (推荐使用 pnpm)

### 安装依赖

```bash
# 使用 npm
npm install

# 或使用 pnpm (推荐)
pnpm install

# 或使用 yarn
yarn install
```

### 配置环境变量

复制 `.env.example` 为 `.env.development` 并根据实际情况修改:

```bash
cp .env.example .env.development
```

编辑 `.env.development`:

```bash
# 开发环境配置
VITE_APP_TITLE=企业级 CRM/ERP 系统 (开发环境)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_API_TIMEOUT=30000
VITE_TOKEN_KEY=crm_erp_token
VITE_ENABLE_MOCK=true
VITE_UPLOAD_MAX_SIZE=10
VITE_PAGE_SIZE=20
VITE_SHOW_DEBUG=true
```

### 启动开发服务器

```bash
# 启动开发服务器
npm run dev

# 或
pnpm dev
```

访问 http://localhost:5173

### 测试账号

开发环境默认提供以下测试账号:

| 角色 | 用户名 | 密码 | 说明 |
|-----|-------|------|------|
| 超级管理员 | admin | admin123 | 拥有所有权限 |
| 销售总监 | sales_manager | 123456 | 销售模块管理权限 |
| 销售员工 | sales | 123456 | 仅查看自己的客户和订单 |
| 库管员 | warehouse | 123456 | 库存管理权限 |
| 采购专员 | purchase | 123456 | 采购管理权限 |
| 财务 | finance | 123456 | 查看财务数据权限 |

## 生产构建

### 构建命令

```bash
# 生产环境构建
npm run build

# 或
pnpm build
```

构建产物输出到 `dist/` 目录

### 预览构建结果

```bash
npm run preview
```

## 部署指南

### Nginx 部署配置

1. **构建项目**

```bash
npm run build
```

2. **配置 Nginx**

创建 Nginx 配置文件 `/etc/nginx/conf.d/crm-erp.conf`:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 项目根目录
    root /var/www/crm-erp/dist;
    index index.html;
    
    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # SPA 路由配置
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理 (可选,如果 API 在同一服务器)
    location /api/ {
        proxy_pass http://localhost:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

3. **重启 Nginx**

```bash
sudo nginx -t
sudo systemctl restart nginx
```

### Docker 部署

创建 `Dockerfile`:

```dockerfile
FROM node:18-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

构建和运行:

```bash
docker build -t crm-erp-web .
docker run -p 80:80 crm-erp-web
```

## 开发规范

### 代码规范

- 使用 ESLint 进行代码检查
- 遵循 Airbnb JavaScript 编码规范
- 组件使用 PascalCase 命名
- 函数和变量使用 camelCase 命名
- 常量使用 UPPER_CASE 命名

### 组件开发规范

1. **组件拆分原则**
   - 页面组件:放在 `pages/` 目录
   - 业务组件:放在 `components/business/` 目录
   - 公共组件:放在 `components/common/` 目录

2. **Props 传递**
   - 使用解构赋值接收 props
   - 复杂对象使用 PropTypes 或 TypeScript 类型定义

3. **状态管理**
   - 全局状态使用 Zustand
   - 服务端状态使用 React Query
   - 组件内部状态使用 useState

### Git 提交规范

使用约定式提交 (Conventional Commits):

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关

示例:

```bash
git commit -m "feat: 添加客户列表筛选功能"
git commit -m "fix: 修复订单状态更新失败的问题"
```

## 常见问题

### 1. 依赖安装失败

**问题**: npm install 失败

**解决方案**:

```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 2. 启动报错

**问题**: 端口被占用

**解决方案**:

```bash
# 修改 vite.config.js 中的端口
server: {
  port: 5174, // 改为其他端口
}
```

### 3. 接口请求失败

**问题**: API 请求返回 404 或 CORS 错误

**解决方案**:

- 检查 `.env.development` 中的 `VITE_API_BASE_URL` 配置
- 确认后端服务已启动
- 检查 `vite.config.js` 中的代理配置

### 4. 打包后样式丢失

**问题**: 生产环境样式不正常

**解决方案**:

- 检查 Tailwind CSS 配置中的 `content` 路径
- 确保所有样式文件已正确导入

## 性能优化建议

1. **路由懒加载**: 已实现,使用 `React.lazy()` 和 `Suspense`
2. **组件懒加载**: 非首屏组件使用懒加载
3. **图片优化**: 压缩图片,使用 WebP 格式
4. **代码分割**: Vite 已自动配置 chunk 分割
5. **缓存策略**: 使用 React Query 缓存服务端数据

## 后续迭代计划

- [ ] 增强数据可视化:更多图表类型和自定义选项
- [ ] 高级审批流:工作流引擎集成
- [ ] 多人协同:WebSocket 实时同步
- [ ] 移动端适配:完整的移动端响应式布局
- [ ] 国际化支持:多语言切换
- [ ] 主题定制:更多主题色方案
- [ ] 性能监控:集成性能监控工具
- [ ] 单元测试:提高测试覆盖率

## 技术支持

如有问题或建议,请联系:

- 📧 Email: support@example.com
- 📱 电话: 400-xxx-xxxx
- 💬 在线客服: https://example.com/support

## 许可证

MIT License

Copyright (c) 2024 Enterprise Development Team

---

**注意**: 本项目仅为前端演示项目,实际部署需要配套后端服务支持。
